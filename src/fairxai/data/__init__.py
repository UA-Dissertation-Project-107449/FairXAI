"""Data package for loading, schema harmonization, and preprocessing.

Public submodules provide dataset loading (`loaders`), preprocessing and
splitting (`preprocessors`), profiling (`profilers`), and schema helpers
(`schemas`).
"""

__all__ = [
    "loaders",
    "preprocessors",
    "profilers",
    "schemas",
]
